import { SignUp } from '@clerk/nextjs'

/**
 * Renders Clerk's sign-up user interface.
 *
 * @returns A JSX element containing the Clerk `SignUp` component.
 */
export default function Page() {
  return <SignUp />
}